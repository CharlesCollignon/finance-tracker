import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function CalendarScreen() {
  return (
    <PlaceholderScreen
      title="Calendar"
      description="Monthly grid with per-day activity and a day detail sheet. Built in Phase 5 on top of the shared calendar logic in packages/core."
    />
  );
}
