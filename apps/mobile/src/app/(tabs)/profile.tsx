import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="Account details and destructive actions. Wired to Supabase auth in Phase 3."
    />
  );
}
