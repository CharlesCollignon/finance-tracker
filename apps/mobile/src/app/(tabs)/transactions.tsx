import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function TransactionsScreen() {
  return (
    <PlaceholderScreen
      title="Transactions"
      description="Month-scoped list grouped by date, type filters, add/delete, and the apply-recurring flow. Built in Phase 5."
    />
  );
}
