---
target: apps/web (Operate surfaces)
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 4
target_identity: "file:/home/charles/Code/charles/finance-tracker/apps/web/apps/web"
timestamp: 2026-09-21T12-10-03Z
slug: apps-web
---
Method: dual-agent (design review + detector, isolated). No browser automation available; static evidence only.

## Design Health Score — Operate surfaces: 24/40 (60%, Acceptable)

| # | Heuristic | Score | Key issue |
|---|---|---|---|
| 1 | Visibility of System Status | 3 | Blanket reduced-motion rule kills the refresh spinner with no other feedback (globals.css:317, RefreshButton.tsx:59) |
| 2 | Match System / Real World | 2 | Catalogue breaks CONTEXT.md's own banned words: entry/entries (en.ts:159-208), "recurring rule" (:405), "DCA" (:368) |
| 3 | User Control and Freedom | 2 | No password-reset route exists anywhere; WelcomeFlow has no Back on any step |
| 4 | Consistency and Standards | 2 | Card.tsx:13 uses rounded-control while others use rounded-card; EmptyState border-dashed; three different focus treatments |
| 5 | Error Prevention | 3 | Preview-then-confirm close and typed confirmations, but window.confirm for delete (TransactionForm.tsx:338) |
| 6 | Recognition Rather Than Recall | 3 | Good chips/autocomplete; closing balance is a genuine memory bridge; MonthRead tone is aria-hidden colour only |
| 7 | Flexibility and Efficiency | 3 | n / Cmd+K quick-add, bulk ops, CSV both ways — but Cmd+K advertised nowhere |
| 8 | Aesthetic and Minimalist | 3 | Genuinely restrained, against a ~13-control Ledger toolbar |
| 9 | Error Recovery | 2 | error.tsx recommends sign-out and provides none; ProfileView leaks SUPABASE_SERVICE_ROLE_KEY guidance to end users |
| 10 | Help and Documentation | 1 | No gloss for bearing, kept, reading day, look-through anywhere in Operate |

## Design Specificity
Authored, emphatically, then undermined at the surface layer. ProjectionCard's two tracks, Spine's withheld ring, BearingCards' five families and FigureRow's refusal to fake a destination all encode reasoning recorded in CONTEXT.md. But the auth screens set h1 in Fraunces italic against "a serif heading is not in this system", and QuickAddSheet sets the amount in mono against "figures that are the subject take Fraunces" — the two typographic rules are inverted at the two screens a user meets first.

## Priority Issues

### P1 — ~60 user-facing strings bypass the i18n catalogue
MonthCloseCard.tsx (zero t() calls), MonthCloseSheet.tsx:207,246,256,281, BankInbox.tsx (12 sites), error.tsx, not-found.tsx, LoginForm, SignupForm, PrivacyToggle aria-label, RefreshButton, QuickAddSheet, TransactionsView, SelectionBar, WelcomeFlow, ProfileView. Parity is enforced by fr.ts being typed as typeof en, so the machinery is airtight and these screens simply do not call it. A French reader meets the product's defining moment in English. Command: adapt

### P1 — The Bearing renders a verdict in colour on ~26 figures
BearingCards.tsx:402-412 toneFor() returns Lamplit Gold or destructive from value direction against each datum's sense. bearing-facts.ts declares 17 up-is-good, 9 up-is-bad, 4 neutral. Collides with the Semantic Amount Rule, spends the Rare Accent Rule's budget a dozen times on one screen, and renders the score PRODUCT.md's third refusal forbids. Command: colorize

### P1 — Accessibility cluster against a stated blocking requirement
Bare `n` global keybinding (QuickAddProvider.tsx:101) violates WCAG 2.1 SC 2.1.4 Character Key Shortcuts, Level A. Quick-add amount field is opacity-0 with no focus-within indicator (SC 2.4.7). BranchedNav.tsx:231 inactive sub-nav ~3.2:1. PrivacyToggle and RefreshButton are 36px against a documented 44px floor. tablist/radiogroup ARIA without keyboard handling. Command: harden

### P1 — A user who forgets their password is locked out permanently
No reset route exists; LoginForm offers no forgot-password link; email+password is a first-class option. Command: shape

### P2 — The two typographic rules are inverted at the screens that set the tone
LoginForm.tsx:39 and SignupForm.tsx:38 set h1 in font-serif italic. QuickAddSheet sets the amount in font-mono text-5xl. Command: typeset

## Detector evidence
17 advisories, 0 blocking, across 128 files. 15 font-size, 2 colour. An undocumented sub-text-xs tier written four ways for two sizes (10px, 0.65rem, 11px, 0.6875rem) across 8 files. AppBackdrop.tsx carries 5 undocumented violet/magenta literals behind every screen. shadow-lg at 5 sites resolves to none — dead markup. MoneyOnHand.tsx:306 holds the only vestigial dark: colour pair.

## Strengths
1. The withheld figure — Spine returns null for an absent ring, FigureRow draws no affordance without an honest destination, Panel's Footer renders nothing when nothing leads anywhere.
2. i18n architecture: parity is a compile-time guarantee; MonthRead carries the read's locale separately from the reader's.
3. ProjectionCard's two tracks told apart by stroke, not colour — survives colour-blindness and greyscale.
