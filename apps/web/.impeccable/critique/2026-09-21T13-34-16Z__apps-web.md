---
target: apps/web (Operate surfaces)
total_score: 23
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 3
target_identity: "file:/home/charles/Code/charles/finance-tracker/apps/web/apps/web"
timestamp: 2026-09-21T13-34-16Z
slug: apps-web
---
Method: dual-agent (design review + detector, isolated, neither told what had changed). No browser automation; static evidence only.

## Design Health Score — Operate surfaces: 23/40 (57.5%, Acceptable)

| # | Heuristic | Score | Was | Key issue |
|---|---|---|---|---|
| 1 | Visibility of System Status | 3 | 3 | Auth errors have no live region; the look-through walk has no progress or cancel |
| 2 | Match System / Real World | 2 | 2 | ~38 hardcoded English strings remain, incl. Date and Note on the add-transaction form |
| 3 | User Control and Freedom | 2 | 2 | WelcomeFlow has no Back, is state-not-URL, and discards typed input |
| 4 | Consistency and Standards | 2 | 2 | Code contradicts DESIGN.md on glass, shadows, focus rings, card radius, money typeface |
| 5 | Error Prevention | 3 | 3 | Preview-before-write is exemplary; Continue silently discards input in onboarding |
| 6 | Recognition Rather Than Recall | 3 | 3 | Month grid markers are aria-hidden colour-only dots |
| 7 | Flexibility and Efficiency | 2 | 3 | REGRESSION: the only keyboard hint advertised N, which this session removed |
| 8 | Aesthetic and Minimalist | 3 | 3 | Bearing disciplined; Ledger stacks ~16 controls above the first row |
| 9 | Error Recovery | 2 | 2 | error.tsx is generic; sign-in failure is unannounced |
| 10 | Help and Documentation | 1 | 1 | No gloss for any invented term anywhere in Operate |

Nine heuristics held. The single point lost is a regression introduced by this session's own fix and has since been corrected in SideNav.tsx.

## What verifiably improved
- The bare `n` binding is gone (WCAG 2.1 SC 2.1.4).
- The Bearing no longer colours figures good/bad; toneFor keeps red only where being under is actionable.
- Three alpha-dimmed foregrounds raised to the full token.
- Two header controls at 44px.
- tablist/radiogroup replaced with aria-pressed on the two sites in scope.
- A password reset route exists.
- Detector findings 17 to 15; the two colour findings cleared because AppBackdrop is now documented.

## Priority issues

### P0 — Four more WCAG contrast failures of the class already fixed twice
MonthPicker.tsx:271 future-month buttons at 2.59:1 (clickable, not disabled); CalendarView.tsx:275 out-of-month days at 2.59:1 (clickable); RecurringView.tsx:117 prose at 3.90:1; QuickAddSheet.tsx:266 amount placeholder at 2.08:1. The project has retired this exact pattern twice with the arithmetic in the comment, and the same value survives four hops away. Command: audit

### P1 — The privacy blur misses 41 figures across 10 surfaces
MonthCloseSheet renders twelve amounts with zero privacy markers. Three places leak amounts into aria-label, which the CSS blur cannot reach. Command: harden

### P1 — ~38 user-facing strings still hardcoded English
The first pass fixed the screens the baseline listed; six components have no t() call at all. Command: clarify

### P1 — The welcome wizard has no Back and silently discards typed input
Continue and Skip for now call the identical handler eight lines apart. Command: onboard

### P2 — Document and code disagree on five named rules
lib/glass.ts makes every card translucent while DESIGN.md says web surfaces are opaque; dead shadow-xl at three call sites; three focus treatments; serif italic on three auth headings; card radius 10px against a documented 20px.

## Detector evidence
15 advisories, all design-system-font-size, 0 blocking. Grep found what the detector cannot: 14 sign-based amount colourings across 9 files, ~23 accent roles against 4 permitted, 34 unmarked money renders, 63 untranslated strings, 3 unfulfilled ARIA role contracts.
