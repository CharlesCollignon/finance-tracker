import { createMistralCategoryReadSource } from "@/lib/category-read/client";

/** The web app's live adapter for the category read seam. */
export const categoryReadSource = createMistralCategoryReadSource();
